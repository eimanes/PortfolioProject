{
    "name": "multi-package-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "concurrently \"npm run build --prefix frontend\" \"npm run build --prefix server\" \"npm run build --prefix websocket\"",
        "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix server\" \"npm start --prefix websocket\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
